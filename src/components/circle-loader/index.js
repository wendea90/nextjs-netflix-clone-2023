

//we do this for if the session is undefined do the circleloader to implement go to context->index.js apply const {data..} & is....

export default function CircleLoader() {
    return <div id="circle-loader" className="circleLoader"></div>
}